

export default {

	namespace: 'tagsDefined',

	state: {

	},

	subscriptions: {
		setup({ dispatch, history }) {  // eslint-disable-line
		},
	},

	effects: {
		// *treeSelect({ payload }, { call, put }) {  // eslint-disable-line
		// 	const res = yield call(Service.treeSelect);
    //   yield put({ type: 'initCondListData', op: 'qygm', listData: res });
		// },
	},

	reducers: {
		

	}
};
